name: Temporary Ghanaian topology narration render

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  render-shard-4:
    name: Render Ghanaian male English shard 4
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install renderer dependencies
        run: python -m pip install --upgrade edge-tts numpy requests
      - name: Render Ghanaian narration shard 4 without FFmpeg
        run: python tools/render_topology_ghanaian_shard4_pure.py
      - name: Upload narration shard
        uses: actions/upload-artifact@v4
        with:
          name: topology-ghanaian-male-english-shard-4
          path: |
            output/topology_ghanaian_shard_4.wav
            output/topology_ghanaian_shard_4.json
          if-no-files-found: error
          retention-days: 1
